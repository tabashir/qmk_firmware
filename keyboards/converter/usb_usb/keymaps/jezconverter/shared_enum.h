#pragma once

enum keyboard_layer {
    // Base layers
    _QWERTY,
    // Intermediate layers
    _LAYER_CHOOSER,
    _IMPACT,
    // Adjust layer at the end
    _ADJUST
};

enum {
    TD_LAYER_FN,
    TD_INSERT_COPY_PASTE,
    TD_ESC_ZERO
};
